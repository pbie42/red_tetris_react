// import Enzyme from 'enzyme'
// import Adapter from 'enzyme-adapter-react-16'
// Enzyme.configure({ adapter: new Adapter() })
import React from 'react'
import { expect } from 'chai'
import { mount } from 'enzyme'
// import { JSDOM } from 'jsdom'
// const dom = new JSDOM('<!DOCTYPE html><html><head></head><body></body></html>')

// global.window = dom.window
// global.document = dom.window.document
import FormHome from '../../src/components/home/FormHome'

describe('FormHome', () => {
	// let props
	// let mountedFormHome
	// const homeForm = () => {
	// 	// if (!mountedFormHome) {
	// 	mountedFormHome = mount(<FormHome />)
	// 	// }
	// 	return mountedFormHome
	// }
	// beforeEach(() => {
	// 	// props = {
	// 	// 	history: Object
	// 	// }
	// 	mountedFormHome = undefined
	// })
	// it('always renders a form', () => {
	// 	// const forms = homeForm().find('form')
	// 	// expect(forms.length).to.be.above(0)
	// })
})
